

class ImgProcessor():

    def init():
        #setup variable
        #network
        #setup parking spots

    def process_frame(frame):
        #cars = self.detect_cars(....)
        #available_spots = self.detect_available(...)

        #return available parking spots(results)

    def detect_cars(frame):
        #detect cars
        #return bounding boxes

    def detect_available(cars, spots):



        #return

    def format_spots(avail):
        #return all_spots
